%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% Short Sectioned Assignment
% LaTeX Template
% Version 1.0 (5/5/12)
%
% This template has been downloaded from:
% http://www.LaTeXTemplates.com
%
% Original author:
% Frits Wenneker (http://www.howtotex.com)
%
% License:
% CC BY-NC-SA 3.0 (http://creativecommons.org/licenses/by-nc-sa/3.0/)
%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

%----------------------------------------------------------------------------------------
%	PACKAGES AND OTHER DOCUMENT CONFIGURATIONS
%----------------------------------------------------------------------------------------

\documentclass[paper=a4, fontsize=11pt]{scrartcl} % A4 paper and 11pt font size

\usepackage[T1]{fontenc} % Use 8-bit encoding that has 256 glyphs
\usepackage{fourier} % Use the Adobe Utopia font for the document - comment this line to return to the LaTeX default
\usepackage[english]{babel} % English language/hyphenation
\usepackage{amsmath,amsfonts,amsthm,bm} % Math packages

\usepackage{sectsty} % Allows customizing section commands
\allsectionsfont{\centering \normalfont\scshape} % Make all sections centered, the default font and small caps
\usepackage{url}
\usepackage{fancyhdr} % Custom headers and footers
\pagestyle{fancyplain} % Makes all pages in the document conform to the custom headers and footers
\fancyhead{} % No page header - if you want one, create it in the same way as the footers below
\fancyfoot[L]{} % Empty left footer
\fancyfoot[C]{} % Empty center footer
\fancyfoot[R]{\thepage} % Page numbering for right footer
\renewcommand{\headrulewidth}{0pt} % Remove header underlines
\renewcommand{\footrulewidth}{0pt} % Remove footer underlines
\setlength{\headheight}{13.6pt} % Customize the height of the header

\numberwithin{equation}{section} % Number equations within sections (i.e. 1.1, 1.2, 2.1, 2.2 instead of 1, 2, 3, 4)
\numberwithin{figure}{section} % Number figures within sections (i.e. 1.1, 1.2, 2.1, 2.2 instead of 1, 2, 3, 4)
\numberwithin{table}{section} % Number tables within sections (i.e. 1.1, 1.2, 2.1, 2.2 instead of 1, 2, 3, 4)

\setlength\parindent{0pt} % Removes all indentation from paragraphs - comment this line for an assignment with lots of text
\def \cov {\text{Cov}}
\def \var {\text{Var}}
\def \arg {\text{arg}}
\def \logit {\text{logit}}

\title{notes of GARCH}
% GARCH Models Structure Statistical Inference and Financial Applications. 2010. Christian Francq. Jean-Michel Zakoı̈an.
\author{sogapalag}

\date{\normalsize\today}

\begin{document}

\maketitle
% weak noise, uncorrelated
% strong noise, indep
% finance: r^2, acf>0; Volatility clustering; Fat-tailed distributions,leptokurticity,(kurtosis); Leverage effects（fear）; Seasonality(calendar effects).
% Family stochastic volatility
% 严格来说GARCH不是stochastic V, 因为given F_{t-1}, 波动是确定的
% Markov representation; 即整个block作为状态，Lyapunov exponent
GARCH$(p,q)$\\
% 在某些条件下，存在S.解
% Thm2.1, GARCH(1,1) (gamma<0), unique(nonanticipative and ergodic.) (strictly)stationary solution. 
% Thm2.4, GARCH(p,q).
% Thm2.5, \sum alpha+beta<1 iff second-order S.
% sum <1 => gamma<0
% def, IGARCH, sum=1, coll2.4 IGARCH(1,1), P(^2=1)<1 => unique S. solution
% ARCH(infty), Thm2.6
% Thm2.8, GARCH(p,q) 属于ARCH(infty). 即用tech (1-b-...)去除
% ACF(r^2) geq 0
\end{document}